{% if call.args %}
    {{ call.variable|raw }}->method('{{ call.function|raw }}')->with({{ call.args|raw }})->willReturn(?); //TODO
{% else %}
    {{ call.variable|raw }}->method('{{ call.function|raw }}')->willReturn(?); //TODO
{% endif %}